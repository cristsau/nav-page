{
  "name": "nav-page",
  "version": "1.0.0",
  "private": true,
  "description": "轻量化、Notion风格的个人导航页",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "dev:app": "npm --prefix app run dev",
    "dev:api": "npm --prefix api run dev",
    "build": "npm --prefix app run build",
    "preview": "npm --prefix app run preview",
    "lint": "npm --prefix app run lint",
    "migrate:api": "npm --prefix api run migrate",
    "start:api": "npm --prefix api run start"
  },
  "keywords": [
    "navigation",
    "bookmarks",
    "notion",
    "vue3",
    "vite"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
