# Project entry

Read CURRENT.md first after required instructions. It is the current-state entry; README/STATUS/HANDOFF retain detailed or historical evidence, not a second current truth. Use this real repository as the working directory, preserve dirty changes, and never reconstruct progress from old task transcripts.

Use scripts/Invoke-CheckSummary.ps1 with the smallest relevant profile; no arguments starts the cheap local profile. Its PASS covers only selected checks. Expand verification for affected dependencies or release/security risk; no automatic remote validation, deployment or cleanup. Full logs remain local, not pasted into context. Keep model/effort unchanged and default to one agent.

At a real milestone update CURRENT.md with independently verified facts, evidence, incomplete gates and at most three next actions. Do not copy the full history into it. Preserve global Domo knowledge ownership, encryption, privacy and production-authorization rules; temporary handoff packets are not knowledge sources.
