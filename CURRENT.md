# DOMO NAV 当前入口

更新：2026-09-08 08:26 +08:00。归属：个人。真实目录 `D:\DomoCodex\projects\NAV-security-first-batch-20260904`。
分支 `codex/nav-security-first-batch-20260904`；运行实现与测试已推送为 `c8269ba48c5fa6f8d60fdf6e050565e1c2c518c9`（含R1及R2两批），原有项目文档/原型正在同分支整理上传；未合并 master。保留无关 dirty，不回旧 NAV 目录、不拼接历史工作树。

## 最新发布推进（2026-09-08）

- 用户明确要求“上传并发布”。`SOURCE_PUSHED / CI_RUNNING / NOT_DEPLOYED`；候选 [CI 34173155958](https://github.com/cristsau/nav-page/actions/runs/34173155958) 已开始，旧R1 CI不替代本轮候选结果。
- 08:21–08:23现场只读：current仍为`20260907-140500-b2ae023`、rollback为`20260903-160400-94543f6`，API/Web/PG健康、旧邮件worker停止。根盘可用7,298,920,448字节/64%已用；双域HTTPS健康200。检查只输出运行元数据和文件权限，不读取秘密。
- 新认证密钥文件 `/etc/nav/integrations/auth-email-keys.json` 尚不存在；真实收件P-03未完成。已请求用户选择“先发布其他功能、验证码保持关闭”或“先完成专用收件验证再完整发布”，未把预选项当作答复。未生成/读取生产密钥、发送邮件、迁移或切换服务。
- 发布前必须再验证精确候选CI、现场备份/PG16隔离恢复及兼容回滚版本；只允许NAV API/Web范围，不重建PG、代理或其他应用，不恢复旧邮件worker，不删除备份。真实手机和24–48h观察仍不冒充自动通过。

## 最近已记录发布

- `DEPLOYED / CORE_ACCEPTANCE_PASS / PHYSICAL_DEVICE_ACCEPTANCE_PENDING`。
- 2026-09-07 14:27 +08:00 发布 merge SHA `b2ae02360d7837f0b5c21daaaa855d986b600b2b`，PR #69；master CI `34088401099` 11/11。本地分支 HEAD 与 merge SHA 不同不是自动判定漂移的依据。
- current `/opt/nav-stack/releases/20260907-140500-b2ae023`；rollback `/opt/nav-stack/releases/20260903-160400-94543f6`。这是 [14:31 发布台账](docs/NAV_RELEASE_20260907_MAIL_RETIREMENT.md) 的证据，不是本次重新远程探测。
- 个人邮箱退役：旧接口 410 / MAILBOX_RETIRED，邮件 worker 停止并保留回滚。导航、时光、媒体、非邮件 AI、Google 身份能力、Web Push 与系统 SMTP 保留，不恢复旧邮件功能。
- 仅重建 API/Web；未执行新迁移、未删历史数据/镜像/备份，其他服务不变。发布前后 PostgreSQL 16 隔离恢复、双域核心 API/AI 日记及验收账号清理通过。

## 最新生产变更：磁盘清理（2026-09-07 19:45）

- 用户要求先清理磁盘；`CLEANUP_APPLIED / TECHNICAL_ACCEPTANCE_PASS`，仅 `ovh-US`，不涉及本机或其他服务器。
- 删除 6 个非 current/rollback、无容器引用的旧 NAV 镜像、超过24小时的闲置构建缓存和两个 APT 索引缓存；实际释放 5,926,047,744 字节（5.52 GiB）。根盘可用从1.4 GiB增至7.0 GiB，使用率93%降至64%。
- 所有容器身份/启动时间/重启次数/排序后挂载保持不变；双域健康和登录页200、匿名session为空、受保护sessions为401。当前/回滚和固定恢复镜像存在，最近发布后备份manifest前后通过。
- 未删数据库、卷、备份、旧release、源码归档或日志，未重启服务。详见[清理台账](docs/NAV_DISK_CLEANUP_20260907.md)。旧镜像删除不能直接撤销，源码可重建但不保证旧digest；当前与回滚完整保留。

## 本轮方案交付及后续实现（2026-09-07）

- 所有者反馈已完成密码更改并登录；未据此宣称双域分别验收完成，不保存密码或恢复执行载荷。
- `CANDIDATE_CI_PASS / NOT_RELEASED / LIVE_ACCEPTANCE_PENDING`：[完整项目方案](docs/NAV_PRODUCT_SPEC_20260907.md)覆盖取消 Passkey、邮箱绑定/验证码登录/验证码改密、功能与 UI、M0–M7 和验收矩阵。用户已要求根据文档完成发布及验收，随后要求优先清理磁盘；不把此授权误写回仅文档任务。
- [UI 概念原型](docs/prototypes/nav-product-20260907.html)仅用虚构数据模拟首页、登录、改密、安全中心和主题；不是已部署界面。
- 本地文档检查：4 份入口/方案文档，71 个本地链接、44 项验收编号和12项功能条目检查通过。Edge 152 原型20组视口/页面布局、核心演示交互和减少动效检查通过，3张截图已目视复核；不等于生产、SMTP或真机验收。
- R1 本地已接入邮箱登录/找回/改密/绑定和换绑、独立加密认证投递、Passkey 固定退役、敏感操作一次性密码复核、认证 UI 与管理员聚合状态。原有密码/恢复码路径统一撤销旧挑战和恢复码；旧个人邮件 worker 不恢复。历史迁移不修改，新增 047。没有生产迁移或新认证发布。
- 最新本地 Full：887 项，874 通过、13 环境条件跳过、0 失败；PostgreSQL 16 无生产挂载/无外网隔离集成 28/28 与迁移 schema 验证通过；旧 001–046 库带合成账号升级 047 通过。
- 后端模式前端构建通过；Edge 实际认证页 54 组模拟 API 布局/交互/抽样对比度检查通过，涵盖 320–1440px 和浅深/自定义配色；敏感操作弹窗另有8项检查通过；不等于真机、真实 SMTP 或双域验收。API/前端依赖审计 0 漏洞，原生依赖本地 smoke 通过，Debian 生产镜像 CI 通过。
- 实现候选 `70bcc53` CI 11/11 已通过；收尾候选 `5699e22` CI [34124678685](https://github.com/cristsau/nav-page/actions/runs/34124678685) 首次10/11，1项因 NuGet 下载 ONNX 超时未进入测试，仅重跑该失败作业后最终11/11通过。精确候选 `5699e22` 的镜像、认证/恢复/其他既有回归已由 CI 验证，但未合并或部署。
- 20:57生产只读复核仍为原 current/API镜像 `b2ae023`、旧邮件 worker exited、双域健康200；可用7,388,880,896字节/64%已用。已精确移除本轮7个临时测试归档（本地副本保留），测试容器及本地预览进程均已收口。
- 详细实施、验收映射和未完成门禁见 [R1 验收记录](docs/NAV_R1_ACCEPTANCE_20260907.md)。保留原有 dirty，未将源代码和模拟测试标为线上完成。

## R2 第一批记录（2026-09-07 22:00）

- 最新用户决定：“现在不能，继续开发，明天统一验收”。真人/真实收件验收留到 **2026-09-08**；今天继续独立本地开发，不以等待收件阻塞 R2，也不提前切换生产。
- `R2_BATCH_1_LOCAL_VERIFIED / NOT_COMMITTED / NOT_PUSHED / NOT_RELEASED`。补齐首屏添加、全设备常显卡片菜单、两行长标题、分组删除数量/不可撤销说明；修复快速收藏与 API 的整 URL 小写误去重，按原有本人/分组边界保留路径、查询、片段大小写，返回既有收藏并可定位。
- 搜索增加全部/书签/笔记/Web 范围切换、失败重试和零结果动作；修复清空输入后迟到响应回写、键盘误选旧结果与输入法组合态触发。Web 仍仅显式动作发送，不随输入发出。
- 复现 1000 收藏切组本地 p95 217.6ms 后，改为每组首批40项、按需加载与 favicon 懒加载；分组总数、全组批选和排序数据不截断，深链接可自动展开第95项，异步加载时序回归通过。相同30次实验最终 p95 108.6ms，中位84.4ms（本机无节流 Edge，不是生产 INP 或完整性能验收）。
- 全量单测890项：877通过/13环境条件跳过/0失败；分页后定向回归通过；最终 backend-mode 前端构建和 `git diff --check` 通过。实际 Vue 浏览器回归18项通过，含5宽度、系统暗色/自定义token、40→80加载、第95项定位、取消/失败保留选择、20次真实 IndexedDB 并发只创建1条。
- 本轮没有远程探测、真实邮件、生产数据库/迁移/重启/清理、提交或推送；R1 的 CI11/11不能覆盖这批未提交代码。完整范围、证据和明日清单见 [R2 第一批验收记录](docs/NAV_R2_ACCEPTANCE_20260907.md)。F-01..04为分批完成，F-05..12及 PostgreSQL 新去重实测/真机/全主题对比度/受限性能仍待后续，不标整个R2完成。

## 最新开发进度：R2 第二批（2026-09-08 00:08）

- `R2_BATCH_2_LOCAL_VERIFIED / NOT_COMMITTED / NOT_PUSHED / NOT_RELEASED`。完成常用分组固定/取消（最多20个，独立本人设置键、不改原分组排序），收到保存确认后才更新；读取失败禁止写入，写入失败保留旧值。刷新恢复和失败重试已模拟验证，未写真实账号设置。
- 未保存排序退出增加确认，取消退出保留草稿，确认放弃恢复原顺序；切页/刷新与进行中的整理有保护。原分页/全量排序/批选数据范围保持。
- 修复 NoteEditor 保存中继续输入时“saving被pending覆盖”的重叠请求风险：独立单请求串行等待，后续提交带最新revision；切页等待在途保存和最新草稿。收到空/错误对象回执不宣称保存，409保留草稿并停止自动覆盖，提供重试/复制草稿。复制只包含标题正文，明确提示另行保留标签/附件/提醒。
- 保存显示区分云端确认、本机待同步、本地模式、同步失败和冲突；手动保存入口也使用真实回执。移动端底部保存/恢复区固定可达、44px按钮、Tab限制/Esc关闭与焦点返回。未改Yjs/PG模型，不把客户端状态检查等同双设备实测。
- 最新全量892项：879通过、13环境条件跳过、0失败；首次发现一条旧整组渲染源码断言，补成分页且完整排序集合不截断后精确复测/全量通过。最终UI补丁再跑定向和backend-mode构建通过。导航20项浏览器检查、笔记实际组件+内存路由7项检查通过；只用合成API/保存回执。导航30次切组p95 134.9ms（本地无节流，非线上性能）。
- 详见同一份 [R2 分批验收记录](docs/NAV_R2_ACCEPTANCE_20260907.md) 的第二批段落；新增 `scripts/verify-note-save-r2-ui.mjs` 可复用。未执行远程探测、生产操作、真实邮件、git提交或推送，R1旧CI不覆盖本地R2。

## 下一步最多三项

1. 上传当前项目文档，完成精确候选CI和PR核验；不得用旧SHA的通过成绩覆盖新候选。
2. 确认验证码本次启用范围；启用需两个服务商专用目的地/模板/数量、owner-only认证密钥及真实收件。仅发布其他功能须保留OTP关闭并明确标注未交付启用，不静默跳过门禁。
3. 通过精确备份/隔离恢复、现场容量与兼容回滚门禁后按授权切换NAV API/Web，完成双域技术验收；本人/iPhone及24–48h观察保留人工边界。未完成的R2余项不计入本次已实现范围。

## 验证与授权

精简入口：`pwsh -NoProfile -File scripts/Invoke-CheckSummary.ps1 -Profile Safety`，只运行指定的本地 URL/请求安全测试。指定其他已有单测使用 `-Profile Targeted -TestFile api/test/具体文件.test.js`；`-Profile Full` 才运行完整 API 单测，前端/集成/恢复/发布门禁仍单独保留。
成功回摘要，失败给有限日志摘录和完整本地日志路径；不得把单测 PASS 说成真实 OAuth、通知或生产已验收。

最初文档整理不含生产授权；用户后续已要求根据方案完成发布与验收，并明确优先清理无用磁盘内容。本次仅执行上述核实清理，未发布。后续发布按确切目标、影响、备份/回滚和验收门禁推进；不扩大到其他服务、秘密读取、备份删除或新外部目的地。

详细资料按需读：[最新发布记录](docs/NAV_RELEASE_20260907_MAIL_RETIREMENT.md)、[Google 配置](docs/NAV_GOOGLE_LOGIN_SETUP.md)、[安全控制](docs/NAV_SECURITY_CONTROLS.md)、[历史状态](STATUS_REPORT.md)、[旧完整 HANDOFF](docs/NAV_FULL_FEATURES_AND_HANDOFF.md)。
本页是当前事实的唯一入口；其余文档保留带日期的详细/历史证据，阶段结束优先更新本页，不默认全读历史。
