# R2 分批验收：导航、快速收藏、搜索与笔记保存

更新：2026-09-08 00:08 +08:00。第一批证据保留2026-09-07时间；第二批追加于文末。归属：个人 DOMO NAV。当前事实入口：[CURRENT.md](../CURRENT.md)。

最新上传状态（2026-09-08 09:42，本地状态补记）：`LOCAL_VERIFIED / MERGED / MASTER_CI_PASS / NOT_DEPLOYED`。

后续本地准备：精确master Web产物已下载并通过revision/内部两项SHA-256校验；实际发布包的54组模拟API页面检查通过，桌面/移动截图已目视核对，本地预览已停止。验证脚本新增可选`NAV_UI_DIST_DIR`以读取被测发布包的JS体积。下一轮12封真实功能闭环仅列方案、尚待授权；未追加发信或切换生产。当前细节仍以 CURRENT 为唯一入口。
两批运行代码与测试已推送为 `c8269ba48c5fa6f8d60fdf6e050565e1c2c518c9`，文档/原型为 `f92b371`；[PR #70](https://github.com/cristsau/nav-page/pull/70)已合并为 `6952684221e401eb0d41cc6aa9d53dfe8bab6377`。[master CI](https://github.com/cristsau/nav-page/actions/runs/34173534307)11/11通过，精确前后端产物已生成。分支push CI曾在依赖下载阶段连接失败，仅重跑失败作业后通过；保留失败记录，不跳过测试。API产物下载、GitHub摘要/内部文件/revision校验及候选镜像载入已完成，仅用于独立空PG16的两封收件冒烟；QQ/Gmail各一次SMTP受理，用户已明确确认两者均收到且均未进垃圾箱，首轮双服务商实收通过。逐封精确耗时未记录，不计为完整P-03受控样本或登录/改密验收。临时环境已清理；未迁移或切换生产，未启用生产OTP，正式P-03和现场恢复/回滚仍待，详见唯一当前入口 CURRENT。以下本地开发段落保留各自当时的授权及验证边界，不覆盖后续现场事实。

用户将真人/真实收件集中验收移至 **2026-09-08**，允许今天继续开发。仅前移独立 R2 本地开发，不跳过 R1 发布门禁。本轮没有生产操作、远程检查或真实邮件；不把历史生产状态当作新探测。

## 第一批实现与边界（9月7日；第二批更新见文末）

| 方案项 | 本批交付 | 状态/剩余范围 |
|---|---|---|
| F-01 导航 | 搜索/添加首屏可达；所有尺寸常显44px操作菜单；标题两行且长串不撑破；favicon懒加载；首批40项和加载更多 | 本地验证；常用分组固定未新增，仍为后续 |
| F-02 管理 | 分组删除列实际客户端已加载数量和不可撤销说明；单条删除补不可撤销；服务器失败/取消保留记录与选择；分页不改变全组100项选择上限和完整排序数据 | 取消和批删失败已模拟；全套编辑/移动/并发排序待综合验证；客户端数量不是锁定的服务端删除快照 |
| F-03 快速收藏 | API去重由整URL LOWER改为规范URL精确比较；手机入口不再低级小写比较；本地Dexie读写事务内去重；服务返回created状态；重复时定位原收藏；内存列表按ID合并 | 沿用本人/同分组边界，跨分组可有意保存相同URL；不批量合并/清理历史记录；历史非规范URL不做数据改写 |
| F-04 搜索 | 全部/书签/笔记/Web标签；方向键/首尾键；可重试部分来源失败；零结果新增或换Web；清空/重试/卸载作废旧请求；结果键盘选取不使用加载中的旧结果；输入法组合态保护 | 客户端合成可访问结果通过；新代码下后端权限/搜索索引集成未重跑；未自动向Web提交输入 |
| U-09 / U-12 局部 | 合成1000收藏分10组，每组100；首批40；加载到80与直达95验证；30次切组实际DOM实验 | 仅本机无节流Edge实验，不是受限CPU/移动网络5次冷启动、生产INP或1000笔记后端检索性能成绩 |

不新增数据库迁移、依赖或全局主题token，不修改稳定权限模型，不恢复退役邮件。F-05..12、常用组固定、完整AA/跨浏览器/真机与发布工作仍待推进。

## 复现的问题与修正

1. 快速入口和API将完整URL转小写，导致 `/Guide` 与 `/guide`、查询值 `VALUE` 与 `value`、片段大小写被误认作同一个页面。改用URL解析器的规范序列化，只做其已知等价转换；保留有语义参数/顺序/尾斜杠，拒绝含凭据或控制字符的地址。扩展沿用同一个后端接口，不另造客户端去重协议。
2. 本地创建未在事务内去重，后端返回既有记录时内存数组仍追加。新增可选 `withOutcome`，默认返回书签的老调用保持兼容；去重结果不再次追加同ID。
3. 输入清空分支未更新搜索序号，迟到响应可写回旧结果。所有输入/重试先作废上一请求，卸载也作废；加载中/已收起的旧选中项不得由Enter打开。
4. 100卡片整组挂载切换，30次无节流本地p95 **217.6ms**，预算失败。首批40+懒图片后复测p95 **122.1ms**，最终完整脚本p95 **108.6ms**、中位 **84.4ms**。不同运行有正常波动，不宣称确定的线上加速比例。
5. 分组和收藏分别加载，首次加载的深链接第95项未能展开，浏览器用例超时。增加目标索引就绪监听，原用例复测通过；不删除失败证据或放宽用例目标。

## 执行证据

- `scripts/verify-navigation-r2-ui.mjs`：固定 `http://127.0.0.1:4179`、独立无个人资料的headless上下文、Service Worker禁用，所有 `/api` 响应为合成，外部网络请求直接拦截；读取真实Vue组件与本地Dexie。
- Edge **152.0.4191.53**，Windows本机。宽320/390/768/1024/1440，高844，reduced motion；默认浅色、系统暗色切换、自定义颜色token注入。18项功能/布局检查全过。自定义token不是完整设置编辑器验收，未声称全面对比度/读屏合规。
- 本地性能采样：1440×844，无CPU/网络节流，1000收藏/10组，30次切组点击至两个animation frame后；验证目标组DOM已出现。1000笔记仅合成数据集，搜索API每类返回8项，不代表真实1000笔记索引性能。
- 最终报告及4张合成截图目录：`C:\Users\ASUS\AppData\Local\Temp\nav-r2-ui-ZtCGfc`。前次相同生产代码报告：`...\nav-r2-ui-MtrPYH`。截图仅合成数据；已目视检查桌面与移动布局，不包含用户账号载荷。
- 真正IndexedDB并发：同一用户/组20次并发创建1条、返回相同ID；路径和查询值大小写不同分别创建，最终3条。不是PostgreSQL并发证明。
- 新单测 `api/test/bookmarkUrlIdentity.test.js`、`api/test/bookmarkCreateRoute.test.js`：前后端规范化一致、语义保留、Fastify实际路由+数据库stub的201/200/400/404契约、本人/分组参数及锁前后顺序。stub无数据库网络，不证明真实PG锁与回滚。
- 全量API单测：890项，877通过、13环境条件跳过、0失败。日志 `C:\Users\ASUS\AppData\Local\Temp\CodexLocalChecks\NAV-security-first-batch-20260904-Full-20260907T135452286Z-024158fce8064995aefe60201e3dd4d0.log`。
- 分页调整后精确重跑导航UI/路由/URL单测通过：`...\NAV-security-first-batch-20260904-Targeted-20260907T135657980Z-6a69f60b56784fe2b46ec6e2e666bcdf.log`。不反复运行未变更的全套。
- 最终 `VITE_AUTH_MODE=backend VITE_API_BASE_URL=/api npm run build --prefix app` 通过；`git diff --check`通过。

复用运行：先在此项目开启固定本地端口的 backend-mode Vite，再设置 `NAV_PLAYWRIGHT_MODULE` 为可用的Playwright模块、`NAV_BROWSER_PATH` 为测试浏览器，执行脚本。不要替换为生产域名或个人浏览器上下文。脚本关闭自己的浏览器，本轮本地Vite也应在结束时精确停止。

## 2026-09-08 集中验收清单

先核对实际运行候选和测试环境；未部署的功能不可直接用旧生产页面判定失败或通过。需发布到线上时，先完成候选CI、备份/隔离恢复、密钥/SMTP/范围、回滚和变更门禁。

- [ ] 确认两个服务商的专用收件目的地及有限发送数量，不在聊天中提交密码/验证码/密钥。
- [ ] 按 [R1台账](NAV_R1_ACCEPTANCE_20260907.md) 验证真实收件、用途隔离/过期/重复使用、邮箱登录和两种改密、改密后其他设备退出、错误和旧密码回退；SMTP受理不等于实收。
- [ ] 两个域名分别本人登录/改密；Google只openid/email/profile，Provider双域回调逐条核对；微信不按昵称合并账号。未配置外部Provider不强启。
- [ ] 真iPhone Safari/PWA：键盘/安全区不遮挡操作，密码管理器/粘贴正常，Passkey入口取消，升级缓存和退役接口不回归。
- [ ] 导航：首屏添加、菜单、长标题、40→80加载、搜索范围/重试/零结果、重复收藏定位到后续页；取消删除、失败保留选择、单条移动及排序数据不丢。
- [ ] PostgreSQL实际精确URL去重与并发/所有权、跨用户搜索权限；Firefox/Safari、200%缩放、主题对比度/VoiceOver及受控性能尚未完成，不沿用模拟PASS。
- [ ] 发布后24–48小时观察单独补齐，不能在明天一次点击验收时提前勾完。

只对执行项记PASS；本批不是整个R2已完成或已上线的声明。

## 第二批：常用分组与笔记保存（2026-09-08 00:08）

状态仍为基线5699e22上的 `LOCAL_VERIFIED / NOT_COMMITTED / NOT_PUSHED / NOT_RELEASED`，未修改真实生产或外部账号数据。

### 本批范围

- F-01：固定/取消当前分组、20项上限、已固定组快捷切换。使用独立本人设置键`navigationPinnedGroupsV1`，后端复用现有用户设置接口，本地模式使用当前用户Dexie；不改分组的原有排序。保存返回值匹配后才显示成功，加载失败不接受修改，写入失败保留旧值；未知/已删除/不属于当前已加载分组的ID不会成为快捷入口。跨设备并发设置仍采用现有最后写入规则，未新增CAS。
- F-02：从排序模式退出必须确认未保存修改，拒绝退出保留草稿，确认放弃恢复原顺序；路由离开/刷新防误丢、批量操作进行时不切页。排序保存服务端并发行为未变。
- F-05局部：把单用户未加密笔记保存改为独立Promise串行控制。保存中继续输入不清除in-flight锁；首个成功后更新revision，再提交最新草稿；切页等待当前请求并排空最新修改，不按延时猜测完成。空/不属于该笔记的回执不是成功。
- 显示区分“已保存到云端”“已保存在本机，待同步到云端”“本地模式”“同步失败/冲突”。手动创建/保存同样读实际回执，后台待同步不显示成云端确认。已打开编辑器的后台重连状态刷新与列表滚动恢复仍需后续整体验证。
- 409保留当前草稿，禁止自动/普通保存覆盖，提示复制后再重新打开；临时错误提供显式重试。复制仅标题/正文，明确排除标签、附件和提醒；不是完整JSON草稿备份。新建与加密笔记保留原手动保存及离开确认，不偷偷把明文写进持久化。
- UI：保存/冲突恢复区固定在编辑器底部，恢复按钮至少44px、状态14px，限制Tab、Esc走未保存确认、关闭返回触发点；触屏安全区留白。没有改Yjs协议、后台合并策略或权限模型。

### 测试及失败记录

- `api/test/navigationPinsAndSaveState.test.js`：固定组有效范围/顺序/去重/上限、保存回执各状态；定向通过。
- 全量892项：**879通过、13环境条件跳过、0失败**。日志：`C:\Users\ASUS\AppData\Local\Temp\CodexLocalChecks\NAV-security-first-batch-20260904-Full-20260907T160446970Z-1b7fda050d09441a8d4f626d634f2512.log`（文件时间为UTC，发生在本地9月8日）。
- 首次全量的1项失败来自`visualConvergenceUi.test.js`仍断言旧的整组`activeBookmarks`渲染；更新为`visibleBookmarks`，并补充完整排序总数不截断的断言。先定向，再全量通过；未降低200ms实验预算或删除行为检查。
- 导航可复用脚本现为**20项PASS**，新增固定组的保存/刷新/读失败/写失败保留，以及退出排序确认/恢复；沿用1000收藏、5宽度和先前边界用例。报告`C:\Users\ASUS\AppData\Local\Temp\nav-r2-ui-PahACG\report.json`。30次本机无节流切组中位110.3ms、p95 134.9ms；不等于线上INP/LCP。
- `scripts/verify-note-save-r2-ui.mjs`新增**7项PASS**：实际NoteEditor和内存router，合成延迟/失败/离线保存回执，保存中再输入、最新revision串行、离线状态、临时错误重试、冲突草稿/模拟剪贴板、4宽度可达/焦点限制、离开等在途及最新草稿、空回执防假成功。报告和移动端截图在`C:\Users\ASUS\AppData\Local\Temp\nav-note-save-r2-5O6asK`，已目视复核。
- 编辑器脚本首次因测试壳的OAuth mock缺少`providers.google`导致页面错误而FAIL，补全合成响应后原行为用例通过；该错误不是新笔记保存的生产故障。
- 最终移动UI补丁后，生产力/视觉/状态定向回归通过：`...\NAV-security-first-batch-20260904-Targeted-20260907T160713792Z-e0a24607881a43bfa1785ecc5c9b886f.log`；最终backend-mode前端构建及diff检查通过。
- 所有浏览器使用独立上下文、禁用SW，API仅合成、外部网络拦截。笔记脚本模拟保存回执，不伪称真实IndexedDB队列/两设备/PostgreSQL冲突/协作同步已测；导航原IndexedDB并发用例仍为真实本地事务。

### 本批之后的验收增量

- [ ] 真实候选中固定/取消分组后跨设备刷新；用户设置只影响本人，删除组后不留下可用的错误入口。
- [ ] 未保存排序取消/离开确认；后端拒绝排序时保留明确状态，不假成功。
- [ ] 真机长请求中继续输入、快速切页、断网再联网；确认最新文字和revision，不静默覆盖。
- [ ] 冲突时复制草稿，取消关闭后内容仍在；完整标签/附件/提醒需另行保留。
- [ ] 真正离线队列、多设备合并、列表筛选/滚动恢复与协作权限变化尚待，不用本脚本的模拟PASS替代。

后续仍按时光其余项→图片引用→助理范围→设置/迁移推进；统一真人验收日期保持2026-09-08。
