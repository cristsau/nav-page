# DOMO NAV Status Report

最后更新：2026-08-01

本页只记录已经合并并有生产验收证据的能力。路线图、局部实现和外部依赖分别标为
“部分完成”或“未完成”，避免把代码存在误写成生产闭环。

## 生产基线（已验证）

- GitHub：`cristsau/nav-page`（Private）
- 已合并 PR：
  - [#8 Responses API 与推理强度](https://github.com/cristsau/nav-page/pull/8)
  - [#9 导航维护、到期提醒与批量操作](https://github.com/cristsau/nav-page/pull/9)
  - [#10 NAV 图片库与图片生命周期](https://github.com/cristsau/nav-page/pull/10)
  - [#11 双域写操作 CORS](https://github.com/cristsau/nav-page/pull/11)
  - [#12 图片库查询参数修复](https://github.com/cristsau/nav-page/pull/12)
- 生产提交：`1ee05335977112093586185a3132559394edd472`
- API 镜像：`nav-api:1ee05335977112093586185a3132559394edd472`
- 生产域名：
  - `https://nav.skrskr.net`
  - `https://nav.cristsau.cn`
- 脱敏发布证据：
  `/opt/nav-releases/20260801-162532-1ee05335977112093586185a3132559394edd472/ACCEPTANCE.txt`
- 当前应用回滚基线：`5b4d03744fcf6916142be71dd963aa9339807619`

## 当前待发布候选（尚未生产验收）

本轮候选代码新增了两条加法迁移和对应 UI；在 GitHub CI、合并、备份、隔离恢复与双域
验收全部完成前，下面内容都不能算作当前生产能力：

- `015_media_delete_outcomes.sql`：严格校验并持久化源文件删除、仅解除引用、旧记录、
  原文件已不存在和缓存清理结果；图片库与笔记按真实结果给出差异化提示。
- `016_security_controls.sql`：公开认证、已认证写操作和 AI 使用 PostgreSQL 原子共享限流；
  IP、用户名、用户 ID 与 User-Agent 只以带密钥 HMAC 摘要进入限流或审计表。
- 管理员安全审计面板：支持事件/结果筛选、分页、刷新与最小化 ID/短指纹展示。
- 生产切换新增硬门槛：所有 API 副本必须配置同一份至少 32 字符的
  `NAV_RATE_LIMIT_KEY_SECRET`；缺失时 API 会拒绝启动，不会回退到进程内限流。

## 当前生产已完成

### UI、分享与移动端

- 首页与时光页共用暖色设计 token；公开分享使用独立文章模板。
- 设计化兜底图标、两行标题、常用入口、首页层级、手机操作菜单和单一创建入口已上线。
- iPhone 主屏幕名称与图标、浏览器扩展快速添加、`/quick-add` 和快捷指令入口可用。
- 全站 `Cmd/Ctrl+K` 命令面板支持页面跳转、新建书签/备忘录/日记、到期提醒和站内搜索。

### 导航、笔记与提醒

- 分组和书签支持拖拽排序、批量选择、批量移动与批量删除。
- 失效链接检查可对选中的书签手动执行，并记录最近检查状态。
- 备忘录截止时间已接入到期提醒中心；提醒会在打开 NAV 时同步，可标记已读或完成。
- 笔记/备忘录 AI、导航 AI 分析与标签、全文快速复制、结构化字段悬停复制均可用。
- 首页统一搜索覆盖书签、未加密笔记和 Web；搜索引擎后台支持增删改。

### 图片库与图床生命周期

- `/media` 图片库已上线：瀑布流、搜索、游标分页、状态筛选、预览、引用跳转、
  复制/分享、保留策略、图床对账、删除失败重试均已实现。
- 上传 Token 与库管理 Token 已按最小权限、同一用户目录分离；浏览器不接触 Token，
  NAV 不把图片二进制写入本机磁盘。
- 新上传图片的完整生命周期已通过生产验收：上传、笔记引用、双域分享、移除最后引用、
  自动清理、原始 URL 返回 404、重复删除与对账均正常。
- 对支持长期删除的来源会删除源文件；Telegram 来源受平台时限约束，旧记录或超时记录
  只能撤销图床索引与公开访问，不能声明源消息已经物理删除。

### AI、账号与数据安全

- CLI Proxy 配置仅由服务端持有；模型目录从 `/v1/models` 动态发现并最多展示 6 个候选，
  “自动最新”按可用稳定通用模型选择。2026-08-01 最后实测为 `gpt-5.6-sol`。
- AI 请求使用 Responses API；推理强度和内置联网搜索参数由 NAV 服务端发送。
- 会话列表与单个/其他/全部撤销、一次性恢复码、密码恢复和恢复后撤销全部会话已上线。
- 登录、注册、恢复和已认证写操作已有实例内限流；可信代理使用精确地址白名单。
- 云端 JSON 导出不会导出 API Key、Telegram Token、密码验证器或未知设置。

### 工程与发布

- Vite `6.4.3`、Node 24 CI、API Docker `npm ci`、Docker 构建上下文秘密排除已落地。
- PostgreSQL `001` 至 `014` 的 13 个迁移文件已应用并通过重复执行与结构校验。
- 待发布候选新增 `015` 与 `016`；尚未计入上述生产迁移数量。
- 个人电脑不承担 npm 验收；npm 安装、审计、测试与构建在 GitHub Actions 或服务器独立
  release 目录完成。

## 最新发布与验收证据

- PR #9 至 #12 合并后，生产固定在 `1ee0533`，未从长期生产工作树原地拉取或构建。
- 两个域名的首页、API 健康、图片库首屏、搜索与游标分页均返回预期状态。
- 两个域名的 POST、PUT、PATCH、DELETE CORS 预检均通过。
- 真实图片端到端生命周期通过；删除后的原始图床 URL 返回 404。
- API 与 PostgreSQL 容器保持健康，验收时重启计数均为 0。
- 发布前、发布后备份均通过精确树、校验和、`pg_restore` 目录与无网络隔离恢复；
  恢复结果为 14 张表、13 条迁移完全一致。

## 备份与恢复现状

- 最新发布前备份：`/var/backups/nav/nav-20260801T083019Z-05e4dca822a0`
- 最新发布后备份：`/var/backups/nav/nav-20260801T083455Z-05e4dca822a0`
- 稳定入口与运行手册已具备：
  - `/usr/local/sbin/nav-backup`
  - `/usr/local/sbin/nav-restore-rehearsal`
  - `docs/NAV_BACKUP_RUNBOOK.md`
- 当前状态仍是“本地手动备份 + 隔离恢复演练”；没有启用计划任务、异地上传、
  远端保留清理或失败报警。

## 部分完成

- 图片删除：当前生产尚未展示图床细分结果；待发布候选已完成持久化和真实提示，仍需
  CI、迁移与真实渠道验收。缓存清理不完整会保守提示，但自动重试/报警仍未完成。
- 到期提醒：只在打开 NAV 时同步已到期项目；没有提前提醒、浏览器/系统推送或离线通知。
- 失效链接：已有选中项手动检查；没有后台定时扫描、集中失效清单和自动复查。
- 命令面板：已覆盖页面和主要创建/搜索动作；还不能直接切换主题或 AI 模型。
- 限流：当前生产仍为 API 进程内计数；待发布候选已改为 PostgreSQL 共享计数，仍需
  配置独立 HMAC 密钥并完成并发、429/503 和双域登录验收。
- 数据迁移：安全 JSON 导入导出可用；尚未形成包含图床对象、外部配置和恢复向导的
  一键完整生态恢复，也没有 Chrome/Edge 书签 HTML 与 Markdown 批量导入导出。
- PWA：主屏幕图标和安装元数据可用；尚无 Service Worker、离线模式或推送能力。

## 仍未完成

- R2/restic 异地加密备份、自动计划、失败报警和外部 dead-man。
- 安全审计保留期、异常告警与导出策略；Passkey/WebAuthn 仍需先确定唯一 RP ID。
- 图床 R2 对象和外层代理配置的独立备份与干净环境恢复演练。
- PostgreSQL 中文 BM25、错别字模糊检索、向量语义检索、基于个人数据的 RAG 助理，
  以及 AI 用量/成本面板。
- Notion 式块编辑器、自动保存和版本历史的大重构；继续放在安全与高频体验之后评估。

## 仍建议用户手工验收

以下项目依赖真实账号、不同设备或恢复码，不应由自动化验收代替：

1. 电脑和手机分别经两个域名登录，确认会话列表能正确区分设备与来源。
2. 生成恢复码并离线保存；用可丢弃账号确认一枚恢复码只能使用一次。
3. 下载一次完整 JSON 导出，人工确认可恢复数据范围与页面提示符合预期。
4. 在 iPhone/触屏设备验收书签菜单、命令入口、提醒中心和图片库操作。

## 发布经验与边界

- 含生产专属文件的长期工作树不能执行 `git reset`、清空目录或覆盖式拉取。
- 旧一键脚本不覆盖完整备份、固定 API 镜像、受控迁移和完整回滚，不能作为当前生产入口。
- Windows 归档需固定源码 LF，并在 release 目录按字节复核。
- 正常应用回滚切回旧固定镜像、旧环境和旧前端，但保留已经应用的加法迁移；
  不得用旧整库覆盖发布后的新写入。
