// Kept as a harmless compatibility entry point for stale deployment commands.
// No configuration, database, IMAP connection, timer or worker is initialized.
console.info('DOMO NAV mailbox retired; nav-mail-worker must be stopped and removed from deployment.')
